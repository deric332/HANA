SELECT TOP 200 DISTINCT "VIEW_NAME", "COLUMN_NAME"
FROM "PUBLIC"."VIEW_COLUMNS" 
WHERE ("SCHEMA_NAME" = '_SYS_BIC' AND  UPPER("COLUMN_NAME") LIKE  UPPER('%LTO%'))
ORDER BY "VIEW_NAME" ASC, "COLUMN_NAME" ASC
